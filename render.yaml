services:
  - type: web
    name: room-booking-server
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install && npm rebuild better-sqlite3
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
